# Competitor pricing research — benchmarking scratch data, not source.
# Keep it local; it is not ours to redistribute.
_research/

# Dependencies
node_modules/

# Shopify CLI
.shopify/
*.liquid.bak

# Local env / secrets — never commit an Admin API token
.env
.env.*

# Editor & OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Shopify theme lives in its own repo (theme files must be at repo root for
# Shopify GitHub sync): https://github.com/kartik1605/posterverse-theme
custom-sticker-store/
